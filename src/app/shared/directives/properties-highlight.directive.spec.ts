import { PropertiesHighlightDirective } from './properties-highlight.directive';

describe('PropertiesHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new PropertiesHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
